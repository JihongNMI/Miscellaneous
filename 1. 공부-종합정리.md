사용 버전 

1. Java : JDK 11
2. SQL ( Oracle Database 19c (서버), SQL Developer ver 24.3.1. (클라이언트 도구), DBeaver (클라이언트 도구) )
3. JSP tomcat v10.1(apache-tomcat-10.1.48) / JDK 17 / 내장(JDK 21)
   - jakarta.servlet.jsp.jstl-3.0.1.jar
   - jakarta.servlet.jsp.jstl-api-3.0.0.jar
   - ojdbc11.jar
4. Spring : IntelliJ 2025.2.5 / Tomcat 10.1.48 / JDK 17 / coretto 17
5. React : "react": "^18.3.1", "react-dom": "^18.3.1", "vite": "^7.2.4"
---


교육과정
1. 25년 9월 초~9월 중순 : HTML & CSS & Javascript (웹디자인개발기능사 필기 합격)
  - 

2. 25년 9월 중순 ~ 10월 : 데이터베이스(Oracle SQL), Java
  -  syso
  -  

3. 25년 11월~ : JSP
  - 톰캣
  - 지시어 : <%@ 지시어종류 어쩌구...%> : <%@ page import="java.util.Date"%>, <%@ include file="include.jsp" %>, <%@ taglib ...%>
  - 선언부 : <%! public int add(int n1, int n2) { return n1+n2; } %>, 스크립틀릿 : <% int result = add(10, 20); %>, 표현식 : 결과는 <%= result %>
  - 내장 객체 : 9가지 중 대표적인 것(request.getParameter("name"), response.sendRedirect("Welcome.jsp"), application.getInitParameter("INIT_PARAM") 이건 web.xml(배포 서술자)
  - 내장 객체의 영역 : application > session > request > page, .setAttribute .getAttribute .removeAttribute,
  - 쿠키
  - 세션
  - DB : JDBC(Java Database Connectivity), stmt(Statement), psmt(PreparedStatement), 커넥션 풀(DBConnPool)
    > JDBC로 DB에 연결하고, Statement/PreparedStatement로 정적/동적 SQL을 실행하며, DBConnPool로 연결 관리를 효율화한다.
  - DTO (Data Transfer Object), DAO (Data Access Object)
    > 애플리케이션 계층 $\rightarrow$ DTO를 생성 $\rightarrow$ DAO가 DTO를 받아 $\rightarrow$ DB에 저장/조회 $\rightarrow$ DAO가 결과를 DTO에 담아 반환 $\rightarrow$ 애플리케이션 계층
  - 액션 태그 : <jsp:include page="header.jsp" />
  - model1 방식 게시판 : View랑 CRUD(Create Read Update Delete), 페이징
  - EL : ${ sessionScope.scopeValue }, ${ scopeValue } <- 생략하면 가장 좁은 page영역부터, ${ param.name }, 비교 연산자(gt ge lt le eq ne), 삼항 연산자, 
  - JSTL : <%@ taglib... %>, <c:choose>,
  - 서블릿 : @WebServlet("/list.do"), ... extends HttpServlet, doG(doGet), doP(doPost)
  - 업로드 다운로드 : 
  - model2 방식 게시판
  - 배포 : WAR(Web Application Archive)
  - 웹소켓 : 채팅
  - SMTP : Simple Mail Transfer Protocol, 이메일

4. 25년 12월~(1주일) : Python
  - 기초랑 크롤링 위주

5. 25년 12월~ : Spring, Spring Boot
  - IntelliJ, Postman
  - 사실 gradle 프로젝트를 만들고, 그 안에 build.gradle에 스프링을 집어넣은것임
  - 애너테이션 : @
  - IoC(제어의 역전, Inversion of Control), DI(의존성 주입, Dependency Injection)
  - Bean(빈)
  - 각종 @
  - 프레젠테이션 계층(Controller) - 비즈니스 계층(Service) - 퍼시스턴스 계층(Repository(옛날 DAO)) - DB
  - 자바코드 - ORM(Object-Relational Mapping) - RDB : JPA(Java Persistence API)는 인터페이스, Hibernate는 JPA를 구현한 구현체, 엔티티 매니저(@PersistenceContext, @Autowired)
  - 쿼리 메소드 : findById, findByName, deleteById
  - JPQL : @Query(어쩌구...) 는 책에 없지만 다뤘음
  - QueryDSL : 도 책에 없지만 다뤘음, Impl 
  - 
  - Lombok의 @Builder
  - 순서1 : 엔티티 : @Entity
  - 순서2 : 리포지터리 : BlogRepository
  - 순서3 : 서비스 : @Service 를 하면서 필요에 맞는 DTO(DTO only or Request&Response 따로)
  - 순서4 : 컨트롤러 : @Controller -> 그냥이랑 Axios(/api/어쩌구) JSON
  - 순서5 : (원래는 테스트지만)뷰 : 템플릿 엔진 타임리프 -> 그냥이랑 Axios

  - 순서6: 로그인 보안
    - 스프링시큐리티
    - JWT : 안함
    - OAuth2 : 카카오톡 로그인
  

6. 26년 1월~(약 4주) : 팀프로젝트(약 3주간), AWS 배포 연습(2일)
  - 기획부터 끝까지
  
  - 순서7 : API -> 카카오맵 처음 써봄

  - 
7. 26년 2월~ : React(1.28. 수~)
